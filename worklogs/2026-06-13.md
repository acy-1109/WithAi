# 2026-06-13 워크로그

## 프로젝트 개요
- **프로젝트**: Roguelike Survivor
- **날짜**: 2026-06-13
- **목표**: F1 도움말 및 치트코드 설명 창 구현, 9스테이지 스킵 버그 해결, 엔드리스 모드용 보스 스킵 치트키 보완, 레벨업 창 업그레이드 설명 연동 및 무한 업그레이드 표시 개선

---

## 세션 1: 도움말 및 치트코드 시스템 통합

### 작업 내용

#### 1. F1 도움말 및 치트코드 화면 구현 ([hud.lua](file:///d:/acy/tst/WithAI_Lua/project/src/ui/hud.lua))
- **`HUD.drawHelp(game)` 구현**:
  - 화면 중앙에 프리미엄 글래스모피즘 스타일의 오버레이 팝업 창을 구현했습니다.
  - 게임의 사이버네틱 테마에 맞추어 사이언/블루 네온 글로우 테두리를 적용했습니다.
  - 정보를 효과적으로 전달하기 위해 좌측 열에는 **조작 방법 (CONTROLS)**, 우측 열에는 **치트코드 (CHEAT CODES)**를 보여주는 2단 레이아웃을 구성했습니다.
  - 하단에 마우스 호버 시 크기가 부드럽게 커지는 마이크로 애니메이션이 적용된 **CLOSE** 버튼을 구현했습니다.
  - 마우스 클릭 충돌 판정을 위해 닫기 버튼의 좌표를 `game.helpCloseBtn`에 저장했습니다.

#### 2. 입력 및 게임 루프 제어 ([main.lua](file:///d:/acy/tst/WithAI_Lua/project/src/main.lua))
- **F1 키 토글**: `love.keypressed(key)`에 F1 키 입력을 가로채어 `game.showHelp` 상태를 토글하도록 했습니다.
- **업데이트 일시정지**: 도움말 창이 활성화되어 있을 때 `love.update(dt)`가 조기 반환(early return)되도록 설정하여, 플레이어가 조작법을 읽는 동안 적이 공격하거나 게임이 진행되지 않도록 일시정지 기능을 구현했습니다.
- **ESC 키 닫기**: 도움말 창이 켜져 있을 때 `escape` 키를 누르면 도움말 창이 닫히도록 했습니다.
- **입력 차단**: 도움말 창이 켜진 상태에서는 다른 키보드 조작이나 게임플레이 마우스 이벤트를 차단하여 의도치 않은 작동을 방지했습니다.
- **닫기 버튼 클릭 판정**: `love.mousepressed`에서 닫기 버튼 영역(`game.helpCloseBtn`)을 좌클릭하면 도움말 창이 닫히도록 마우스 입력을 연동했습니다.

#### 3. 다국어 및 텍스트 포맷 정돈
- 새롭게 추가된 UI 문자열과 소스 코드 주석을 한국어에서 영어로 전면 번역 및 교체했습니다.
- 다양한 폰트/플랫폼 환경에서의 호환성과 깔끔한 렌더링을 위해 도움말 화면 내 모든 이모지 특수문자(`🎮`, `⚡`)를 제외하고 `&` 기호를 `AND`로 변경했습니다.

---

## 세션 2: 진행 버그 수정 및 UI 개선

### 작업 내용

#### 1. 9스테이지 스킵 진행 버그 해결 ([main.lua](file:///d:/acy/tst/WithAI_Lua/project/src/main.lua))
- 9스테이지에서 치트키 `K`를 눌러 다음 스테이지로 스킵할 때, 존재하지 않는 10스테이지로 진입을 시도해 발생하던 에러 버그를 수정했습니다.
- 현재 9스테이지일 경우 `K`를 누르면 즉시 스테이지 클리어 화면(`"stage_clear"`, 즉 엔드리스 모드 선택창)으로 이동하고 게임 진행을 일시정지(`game.running = false`)하도록 조건문을 추가했습니다.

#### 2. 치트키 I 엔드리스 모드 연동 수정 ([main.lua](file:///d:/acy/tst/WithAI_Lua/project/src/main.lua), [hud.lua](file:///d:/acy/tst/WithAI_Lua/project/src/ui/hud.lua))
- 보스 웨이브로 건너뛰는 치트키 `I`의 동작을 수정했습니다:
  - 일반 모드에서는 기존처럼 7웨이브(보스 웨이브)로 이동합니다.
  - **엔드리스 모드**에서는 보스가 5웨이브마다 출현하므로, 현재 웨이브 기준 다음 5의 배수 웨이브(예: 5, 10, 15, 20...)로 계산해 즉시 보스 웨이브를 시작하고 보스를 스폰하도록 보완했습니다.
- F1 도움말 설명의 `I` 키 설명을 `"Spawn Boss Wave (Wave 7 / Next 5th Wave)"`로 수정했습니다.

#### 3. 레벨업 화면 업그레이드 설명 연동 및 무한 레벨 표기 개선 ([hud.lua](file:///d:/acy/tst/WithAI_Lua/project/src/ui/hud.lua))
- **동적 레벨업 설명 추가**:
  - `skillLevelDescriptions`에 그동안 누락되어 있던 회전 구체(Orbiting Orb, index 1)의 레벨별 설명을 추가했습니다.
  - 패시브 특성용 설명 룩업 테이블인 `upgradeLevelDescriptions`를 신설하여 자석, 이동 속도, 체력 재생, 경험치 획득 증가, 가시, 에너지 실드의 레벨별 상승 수치를 정의하고, 레벨업 시 다음 획득 레벨에 맞는 설명이 동적으로 출력되도록 수정했습니다.
- **무한 레벨 패시브 UI 개선**:
  - 레벨 제한이 없는 무한 업그레이드 특성(체력 증가 - Health Boost, 공격력 증가 - Damage Boost)은 기존의 3칸짜리 다이아몬드(별) 아이콘 대신 직관적으로 현재 레벨을 텍스트로 보여주도록 `Level: X -> Y` 표기식으로 분기 처리했습니다.

#### 4. 강화 상점 설명 동적 업데이트 연동 ([hud.lua](file:///d:/acy/tst/WithAI_Lua/project/src/ui/hud.lua))
- 강화 상점(메타 업그레이드 상점)에서도 인게임 레벨업 창과 동일하게 현재 업그레이드 레벨에 따른 다음 레벨 설명이 동적으로 표시되도록 보완했습니다.
- 레벨을 올릴 때마다(Buy 클릭 시) 해당 특성의 다음 레벨 성능 설명으로 갱신되며, 최대 레벨(MAX)에 도달하면 마지막 최대 레벨의 설명이 그대로 유지되도록 구현했습니다.

#### 5. 레벨업 화면 레이아웃 및 폰트 튜닝 ([hud.lua](file:///d:/acy/tst/WithAI_Lua/project/src/ui/hud.lua))
- 레벨업 스킬/특성 선택 화면(`HUD.drawUpgrade`) 및 시작 스킬 선택 화면(`HUD.drawMenu`)에서 텍스트가 카드 한계를 벗어나는 현상을 방지하기 위해 폰트 및 Y 좌표 배치를 튜닝했습니다.
- 설명글의 폰트 크기를 기존 `16`에서 **`13`**으로 소폭 감소시켜 가독성과 행간 밀도를 조율했습니다.
- 카드 이름 위치를 위쪽(`0.32`), 설명글 위치를 중간(`0.44`), 그리고 하단의 레벨 지시자/다이아몬드 아이콘 위치를 아래쪽(`0.72`)으로 각각 오프셋 조정하여 수직 여백 공간을 약 40% 이상 넓혀 글자가 겹치거나 넘치던 문제를 해소했습니다.

#### 6. 전체 보스 체력 상향 조정 ([spawner.lua](file:///d:/acy/tst/WithAI_Lua/project/src/enemy/spawner.lua))
- 첫 번째 보스를 포함한 모든 스테이지 보스들이 플레이어의 스펙 업그레이드 속도에 비해 너무 빠르게 격퇴되는 현상을 해결하기 위해, 보스들의 기본 최대 체력(`baseHealth`) 스펙을 일괄 **약 2.85배 상향** 조정했습니다.
  - **Void Overlord (St 1)**: `8,400` -> **`24,000`**
  - **Infernus Leviathan (St 2)**: `14,000` -> **`40,000`**
  - **Phantom Stalker (St 3)**: `22,400` -> **`64,000`**
  - **Tesla Archon (St 4)**: `33,600` -> **`96,000`**
  - **Orbital Aegis (St 5)**: `50,400` -> **`144,000`**
  - **Chronos Weaver (St 6)**: `70,000` -> **`200,000`**
  - **Glitch Overlord (St 7)**: `98,000` -> **`280,000`**
  - **Singularity Nexus (St 8)**: `126,000` -> **`360,000`**
  - **Nebula Seraph (St 9)**: `168,000` -> **`480,000`**
- 5스테이지 보스 체력 버프에 맞춰 주위를 돌며 보스를 보호하는 이지스 실드 소환체(`aegis_shield`)의 기본 최대 체력 또한 기존 `1,500`에서 **`4,500`**으로 동일 비율(3배) 상향 적용했습니다.

---

## 세션 3: 보스 및 소환체 체력 2차 밸런스 조정

### 작업 내용

#### 1. 전체 보스 체력 추가 상향 조정 (2.5배) ([spawner.lua](file:///d:/acy/tst/WithAI_Lua/project/src/enemy/spawner.lua))
- 첫 번째 보스(Void Overlord)가 플레이어의 강화 상태 및 초반 화력 증가 속도에 비해 여전히 너무 빨리 처치되는 현상이 확인되어, 전체 보스들의 기본 최대 체력(`baseHealth`) 스펙을 **추가로 2.5배 상향** 조정했습니다. (최초 대비 총 7.14배 상향)
  - **Void Overlord (St 1)**: `24,000` -> **`60,000`** (2.5배 상향)
  - **Infernus Leviathan (St 2)**: `40,000` -> **`100,000`** (2.5배 상향)
  - **Phantom Stalker (St 3)**: `64,000` -> **`160,000`** (2.5배 상향)
  - **Tesla Archon (St 4)**: `96,000` -> **`240,000`** (2.5배 상향)
  - **Orbital Aegis (St 5)**: `144,000` -> **`360,000`** (2.5배 상향)
  - **Chronos Weaver (St 6)**: `200,000` -> **`500,000`** (2.5배 상향)
  - **Glitch Overlord (St 7)**: `280,000` -> **`700,000`** (2.5배 상향)
  - **Singularity Nexus (St 8)**: `360,000` -> **`900,000`** (2.5배 상향)
  - **Nebula Seraph (St 9)**: `480,000` -> **`1,200,000`** (2.5배 상향)

#### 2. 이지스 실드 소환체 체력 상향 조정 ([spawner.lua](file:///d:/acy/tst/WithAI_Lua/project/src/enemy/spawner.lua))
- 5스테이지 보스 체력의 2.5배 상향 비율에 맞춰, 보스 주변을 회전하며 실드 역할을 하는 위성 소환체(`aegis_shield`)의 체력도 기존 `4,500`에서 **`11,250`**으로 2.5배 상향하여 방어 밸런스를 맞췄습니다.

---

## 세션 4: 3스테이지 보스 순간이동 및 분신 이동 버그 수정

### 작업 내용

#### 1. 분신 뭉침 방지 물리 엔진 제외 처리 ([spawner.lua](file:///d:/acy/tst/WithAI_Lua/project/src/enemy/spawner.lua))
- 3스테이지 보스(Phantom Stalker)가 4분면 순간이동 격자 배치 패턴(`crossfire`)을 실행할 때, 플레이어가 맵 가장자리나 모서리에 위치하면 여러 분신이 한 곳에 겹쳐서 순간이동하게 되는 버그가 있었습니다.
- 겹쳐서 순간이동된 분신들이 몬스터 간 뭉침 방지 물리 연산(`Separation Force`)을 강하게 받아 사방으로 튕겨나가며 휙 사라져버리는 오류가 발생하여, `boss_clone` 타입을 뭉침 방지 연산 예외 리스트에 추가했습니다 (`enemy.type ~= "boss_clone"`).

#### 2. 조준 및 사격 중 분신 이동 고정 처리 ([spawner.lua](file:///d:/acy/tst/WithAI_Lua/project/src/enemy/spawner.lua))
- 분신들이 플레이어 주변 4분면으로 순간이동 배치된 직후 1초 동안 멈춰 서서 레이저로 조준(`charging_crossfire`)하고 사격(`crossfire_firing`)해야 하지만, 일반 몬스터의 추적 코드를 타고 플레이어에게 돌격하여 몸비비기를 시전하는 문제가 있었습니다.
- `spawner.lua` 의 일반 몬스터 이동 루틴에 조건문을 추가하여 `boss_clone`이 조준 및 사격 상태일 때는 `targetVelX, targetVelY, velX, velY` 속도를 강제로 `0`으로 고정시켰습니다.
- 추가로 lerp를 통해 회전 속도가 적용되는 물리 오버라이드 조건에도 `boss_clone`의 해당 공격 모션을 예외로 등록하여 프레임 변화에도 위치가 미끄러지지 않고 완벽히 고정되도록 개선했습니다.

---

## 세션 5: 3스테이지 보스 순간이동 연출 효과 추가

### 작업 내용

#### 1. 이전 위치 홀로그램 잔상 연출 ([spawner.lua](file:///d:/acy/tst/WithAI_Lua/project/src/enemy/spawner.lua))
- 보스 및 분신들이 X자로 재배치되기 직전에 원래 서 있던 위치 좌표를 캡처하여 `game.teleportEchoes` 배열에 등록하는 로직을 신설했습니다.
- 등록된 잔상은 0.4초 동안 크기가 줄어들면서 불투명도가 흐릿해지는 홀로그램 페이드 아웃 연출이 적용되어 렌더링되도록 구현했습니다.

#### 2. 목적지 네온 스파크 파티클 연출 ([spawner.lua](file:///d:/acy/tst/WithAI_Lua/project/src/enemy/spawner.lua))
- 보스와 분신들이 새로운 목적지에 안착하는 프레임에 각 엔티티의 중심 좌표에서 사방으로 12개의 글리치 파티클(`game.teleportParticles`)을 방출하는 폭발 연출을 추가했습니다.
- 이 파티클들은 0.4~0.6초 동안 사방으로 날아가며 서서히 작아지고 흐려집니다.

#### 3. 안착 시 페이드 인 및 확장 애니메이션 연동 ([spawner.lua](file:///d:/acy/tst/WithAI_Lua/project/src/enemy/spawner.lua))
- 순간이동 직후 각 객체에 `teleportFade = 0.3` 타이머를 발급했습니다.
- 드로우 루틴 내에서 이 타이머가 동작하는 동안(0.3초) 알파값(`baseAlpha`)은 0에서 1로 페이드 인 되며, 보스의 외곽 크기(`pulse` 및 `halfW`)와 눈동자 코어 크기(`eyeSizeMult`)는 원래 크기의 50%에서 100%로 확대되며 자연스럽게 나타나도록 애니메이션화했습니다.

---

## 세션 6: 3스테이지 보스 분신 충원 소환 로직 개편

### 작업 내용

#### 1. 생존 분신 감지 및 비어있는 슬롯 매핑 ([spawner.lua](file:///d:/acy/tst/WithAI_Lua/project/src/enemy/spawner.lua))
- 3스테이지 보스(Phantom Stalker)가 분신을 재생성하는 `summon` 상태가 되었을 때, 무조건 3개의 분신을 새로 만들던 기존 로직을 수정했습니다.
- 현재 게임판에 살아있는 이 보스의 분신(`boss_clone`)들의 정보를 조회하여 `cloneSlot` 번호(1, 2, 3)가 사용 중인지 여부를 저장하는 `slotOccupied` 불리언 맵핑 테이블을 구현했습니다.

#### 2. 비어있는 슬롯 대상 분신 보충 소환 ([spawner.lua](file:///d:/acy/tst/WithAI_Lua/project/src/enemy/spawner.lua))
- 슬롯 1부터 3까지 돌면서 `slotOccupied[slot]`이 `false`인 비어있는 슬롯(방향)에 대해서만 새로운 `boss_clone`을 소환하여 `cloneSlot = slot`을 부여하는 보충 소환 방식으로 개편했습니다.
- 이를 통해 살아남은 분신들은 제자리와 체력을 그대로 보존하고, 처치당한 분신이 있던 빈 방향에만 정확하게 추가 소환이 이루어집니다.
- 최대 3개의 분신 제한이 완벽하게 유지됩니다.

---

## 세션 7: 배경 우주 먼지 설정 실시간 적용 버그 수정

### 작업 내용

#### 1. 동적 배경 요소의 showStars 설정 연동 ([main.lua](file:///d:/acy/tst/WithAI_Lua/project/src/main.lua))
- OPTIONS 설정 창의 `Enable Background Star Dust` 설정을 꺼도 배경 먼지들이 여전히 렌더링되던 버그를 고쳤습니다.
- 원인은 `game.showStars` 설정 검사가 1스테이지의 일부 정적인 파편들에만 적용되어 있었고, 정작 화면 전체를 채우는 동적 입자 요소(우주 먼지, 마그마 재, 사이버 기판 노드, 황금 파티클 등)들은 검사 없이 무조건 렌더링되고 있었기 때문입니다.
- `main.lua`의 `love.draw` 함수 내 동적 입자 그리기 루프에 `if game.showStars then` 분기를 추가하여, 옵션을 해제하는 즉시 실시간으로 배경 입자들이 화면에서 전부 사라지고, 켜면 다시 나타나도록 개선했습니다.
## 세션 8: 7, 8, 9 스테이지 배경 우주 먼지 설정 실시간 연동 수정

### 작업 내용

#### 1. 7, 8, 9 스테이지 특수 파티클의 showStars 설정 연동 ([main.lua](file:///d:/acy/tst/WithAI_Lua/project/src/main.lua))
- 1~6 스테이지뿐만 아니라, 고유한 그리기 로직을 사용하는 7, 8, 9 스테이지의 배경 파티클도 `Enable Background Star Dust` 설정을 끌 때 화면에서 사라지도록 연동했습니다.
- 대상 파티클:
  - `glitch_node` (7스테이지 사이버 글리치 노드)
  - `void_node` (8스테이지 특이점 블랙홀 입자)
  - `shimmer_star` (9스테이지 천상의 황금 먼지)
- `main.lua`의 `love.draw` 내부의 각 파티클 렌더링 루틴에 `if game.showStars then` 분기를 감싸주어 설정이 실시간으로 반영되도록 해결했습니다.

## 세션 9: 체인 스킬 보스 슬로우 효과 추가 및 보충 개선

### 작업 내용

#### 1. 체인 타겟팅 시 보스 타겟 허용 및 중복 타겟팅 방지 ([skills.lua](file:///d:/acy/tst/WithAI_Lua/project/src/progression/skills.lua))
- 체인의 적 검색 조건에서 보스를 제외하던 제한을 해제했습니다 (`excludeBoss = false`, `enemy.type ~= "boss"` 검사 제거).
- **중복 타겟팅 방지**: 이미 활성화된 체인의 타겟이 된 적을 검색 목록에서 배제하는 `isAlreadyChained(game, enemy)` 함수를 구현하고 `findClosestEnemies` 및 `findClosestEnemyFrom`에 연동했습니다.
  - 이를 통해 보스에게 다수의 체인이 쿨다운마다 무한정 겹쳐 부착되어 사라지지 않는 현상을 해결하고, 체인의 타겟 분산 효과를 개선했습니다.

#### 2. 보스 피격 시 슬로우 효과 부여 ([skills.lua](file:///d:/acy/tst/WithAI_Lua/project/src/progression/skills.lua))
- 체인이 적에게 안착하는 시점에 대상이 보스(`enemy.type == "boss"`)인 경우 속박(`rootedTimer`) 대신 슬로우 상태값(`slowTimer = rootDuration`, `slowMultiplier = 0.5`)을 부여하도록 수정했습니다.

#### 3. 슬로우 수명 주기 및 최종 변위 감속 통합 ([spawner.lua](file:///d:/acy/tst/WithAI_Lua/project/src/enemy/spawner.lua))
- 적 업데이트 루프(`Enemy.update`)에 `slowTimer` 차감 로직을 구현하고 타임아웃 시 `slowMultiplier = nil`로 정리되도록 설정했습니다.
- **돌진 패턴 감속 보장 및 이중 감속 예방**: 물리 속도(`targetVel`, `maxAllowedSpeed`)에 감속을 직접 곱하던 방식을 걷어내고, 최종 위치를 갱신하는 시점에 `isSlowed` 조건이 참인 경우 최종 이동 속도(`vx, vy`)에 `slowMultiplier` (50%)를 적용하도록 연산을 일괄 통합했습니다.
  - 이를 통해 프레임 단위로 속도를 직접 덮어씌우는 보스의 **대시(Dash) 및 돌진(Rush) 패턴**을 포함한 모든 상태 기동에 대해 누락 없이 완벽한 50% 감속이 보장됩니다.
